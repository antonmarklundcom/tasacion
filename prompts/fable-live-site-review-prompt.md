# Prompt for Fable 5.1 — Live site review of tasacion.com.py

Paste everything below the line into Fable 5.1.

---

You have Playwright browser access. Analyze the live site **https://tasacion.com.py/** and produce a full improvement report, then save a plan/idea file into the project repo.

## Context
- This is a real-estate appraisal ("tasación de inmuebles") lead-gen site for a Paraguayan tasador (Fernando Capurro), serving Asunción y Gran Asunción.
- The site's source code lives at `C:\Claude 1\tasacion-com-py` — a static HTML site generated by `build-site.mjs` from `content.mjs` (no CMS/backend). Read that source after your live audit so recommendations reference real files/sections, not guesses.
- Primary conversion goal: WhatsApp contact (`wa.me` links with pre-filled, page-specific messages) for two paths — paid official appraisal report ("informe oficial de tasación") vs. free valuation for sellers ("valoración gratis para vender").
- Known pages to crawl: `/`, `/tasaciones/`, `/tasaciones/casas/`, `/tasaciones/departamentos/`, `/tasaciones/terrenos/`, `/tasaciones/corporativa/`, `/tasaciones/hipotecaria/`, `/tasaciones/locales-comerciales/`, `/tasaciones/campos/`, `/valuacion-para-vender/`, `/informe-oficial/` (or `/informes-periciales/`), `/nosotros/`, `/preguntas-frecuentes/`, `/contacto/`, `/gracias.html`, `/404.html`.

## What to do
1. **Crawl the live site with Playwright** at both desktop (1440px) and mobile (390px) viewports. For each page capture: a screenshot, page title/meta description, H1/H2 structure, and any console errors or failed network requests.
2. **Check for the known open issue**: `/tasaciones/` (and possibly its child pages) returned a Cloudflare/anti-bot "Checking your browser… 403" challenge when hit directly on 2026-09-06. Confirm whether this still happens, on which exact URLs, and whether it's Cloudflare-specific (check response headers) or a Hostinger-side bot-protection rule. This is the top-priority item to nail down.
3. **Evaluate and score** (1–5 each, with concrete evidence from what you saw):
   - Conversion clarity: is the WhatsApp CTA obvious above the fold on every page? Are the two conversion paths (paid informe vs. free valuación) clearly differentiated?
   - Trust/credibility signals: tasador credentials, sample report, testimonials, legal/bank validity claims — are they substantiated or just asserted?
   - Copy quality: is each vertical page (casas, departamentos, terrenos, corporativa, hipotecaria, locales-comerciales, campos) meaningfully differentiated, or templated filler?
   - Visual design: hierarchy, whitespace, image quality/relevance (note if any placeholder or generic stock imagery remains).
   - Mobile UX: tap targets, nav collapse behavior, WhatsApp floating button placement, readability.
   - Performance: page weight, image formats (AVIF/WebP present?), any render-blocking resources, Core Web Vitals if measurable.
   - SEO basics: title/meta uniqueness per page, heading hierarchy, internal linking between vertical pages and the `/tasaciones/` hub, sitemap.xml/robots.txt sanity, structured data (LocalBusiness/Service schema) presence.
   - Accessibility: color contrast, alt text, focus states, form labeling (contact form).
4. **Cross-reference with source**: for the 2–3 highest-impact issues you find live, open the corresponding file(s) in `C:\Claude 1\tasacion-com-py` (likely `content.mjs`, `build-site.mjs`, `assets/css/site.css`) and note specifically what would need to change and where — don't just describe the symptom.

## Deliverables
1. **Full written report** delivered in chat, organized as: Executive Summary (top 5 issues ranked by impact/effort) → the 403/Cloudflare finding → the 8 scored categories above with evidence → a prioritized backlog (quick wins vs. bigger bets).
2. **Save a plan/idea markdown file** into the repo at `C:\Claude 1\tasacion-com-py\docs\LIVE-SITE-REVIEW-2026-09-07.md` (adjust the date to today) containing the same findings in durable form: dated header, the 403 finding, the scored categories, and a prioritized action list with rough effort estimates. This file is meant to be read by future work sessions on this project, so write it as project documentation, not a chat transcript — no "I found" narration, just findings and recommendations.
3. Do not modify any site source files — this is an audit only, no fixes.
