# Log de build — v3: credenciales reales, finalidades, precios por finalidad

## PR-1 — contrato, renderer, gate (`v3/1-contrato-renderer-gate`)

**Built**
- `content.mjs`: CRED_CSJ/CRED_CSJ_CORTA/CRED_ARQ/CRED_BCP_*/PLAZO_TXT/IVA_TXT/FACTURA_TXT/
  PRECIOS/PRECIO_JUDICIAL_TXT/PRECIO_CREDITO_TXT/FRANJA_COTIZA/VISITA_TXT/EJEMPLO/FINALIDADES.
- `WA_MENU` con las 5 opciones verbatim de §4; `page.waConsultaText` (corporativa/franja).
- Nueva ruta `/tasaciones/franja-de-dominio/` (kind `vertical-b2b`), copy provisional (H1/lede/CTA).
- `build-site.mjs`: `useCases`/`credentials`/`pricingTiers`/`valueBlock`; `priceBlock` parametrizado
  (eyebrow/ctaLabel/waOption/pie); `ctaBand` parametrizable + PLAZO_TXT automático; header fallback
  bug fix (ya no manda "()"); trust row con los 3 ítems nuevos, en todas las páginas indexables;
  JSON-LD ProfessionalService/Service/BreadcrumbList; fonts preload+onload.
- CSS: `.use-cases`/`.credentials`/`.pricing-tiers`/`.value-block`; `.wa-menu__option` a grid
  (stacking fix); `.hero__freelink` padding-right en ≤640 (FAB overlap fix).
- `.htaccess`: `ErrorDocument 404` + 301 www→apex. `docs/routes.json`: 15 rutas, títulos ≤60.
- `verify.mjs` y `tests/wa-menu.mjs` reescritos para 5 opciones, credenciales canónicas, IVA.

**Decisions**
- `docs/routes.json.copyDone` queda en `false` hasta que las 15 páginas tengan la copia v3
  completa (PR-3), no en PR-2 como sugiere la tabla §8 — el gate de IVA/credenciales/forbidden-v3
  no puede exigirse a páginas que todavía no fueron reescritas. Ver Known issues.
- Varios títulos superan 60 caracteres con el copy viejo (home, hub, locales, franja provisional,
  informes-periciales, nosotros, FAQ) — acortados ahora (spec permite acortar títulos).
- Fix mecánico aplicado ya en PR-1 (no espera a PR-2): `porQueElegirnos` ítem 3 reescrito con
  CRED_CSJ/CRED_ARQ (spec dice "se reescribe en todo el sitio"); hero primario de hipotecaria
  cambiado a `credito` (mecánica del menú, no copy narrativo) para que el test de foco por página
  pase ya en PR-1.
- `INCLUYE_INFORME` ganó `Firma de ${CRED_CSJ}` y `Entrega en ${PLAZO_TXT}` (afecta las 7
  verticales con `priceBlockVertical`, aún con precio viejo sin IVA — se corrige en PR-3).

**Known issues**
- Copy vieja (sin IVA, con "peritos matriculados", "estándares bancarios", "Te confirmamos el
  plazo") sigue en casas/departamentos/terrenos/corporativa/hipotecaria/locales/campos/FAQ/etc.
  hasta PR-2/PR-3 — por diseño; el gate v3 (FORBIDDEN_V3, sufijo IVA, FACTURA_TXT) solo se activa
  con `copyDone: true`.
- JSON-LD `Service.offers` con rango judicial como segundo `Offer` en informes-periciales: no
  implementado (solo el offer de compraventa); pendiente para no bloquear PR-1.
- Franja de dominio: copy provisional (solo lead "Cómo se cotiza" + ctaBand); completa en PR-2.

**Verification:** `node verify.mjs` PASS y `node tests/wa-menu.mjs` PASS en local antes de abrir el PR.

## PR-2 — copy hub (`v3/2-copy-hub`)

**Built**
- Copy v3 completa (§5) en: home, `/tasaciones/` (hub), `/tasaciones/hipotecaria/`,
  `/informes-periciales/`, `/valuacion-para-vender/`, `/nosotros/`, `/preguntas-frecuentes/`,
  `/contacto/`, `/tasaciones/franja-de-dominio/`, `404.html`, `gracias.html`.
- `informes-periciales`: `useCases` con 4 finalidades, `lead` #judicial/#credito, `pricingTiers`
  (3 tarjetas verbatim §5.6), `valueBlock` completo, `credentials`, FAQ ampliada.
- Home: `useCases` (#finalidades), `valueBlock` completo, `credentials`, `steps` actualizado.
- Aplicado `prompts/v3-design-addendum.md` (apareció en `master` antes de abrir este PR): CSS
  exacta de `.pricing-tiers`/`.use-cases`/`.credentials`/`.value-block`/`.wa-menu__option`, chip
  "Lo más pedido", tiles de ícono, tarjeta navy de crédito, lede sticky.
- `contacto`: radios = las 5 etiquetas de `WA_MENU` en orden, envueltos en `<fieldset><legend>`.
- Found & fixed durante screenshot QA: cifra del `pricingTiers`/`priceBlock` no llevaba `IVA_TXT`
  en el mismo nodo de texto (ahora sí, en el mismo `<p>`); íconos check del pie de `pricingTiers`
  sin tamaño CSS (renderizaban gigantes) — corregido.

**Decisions**
- `docs/routes.json.copyDone` sigue en `false`: las 6 verticales + corporativa (PR-3) todavía
  tienen copy vieja sin IVA. Verificado manualmente con `copyDone:true` temporal: las páginas de
  este PR pasan todos los checks de copy; solo fallan las de PR-3 (esperado).
- `/informes-periciales/` useCases: la 4ª tarjeta ("Vender con corredor") enlaza a
  `/valuacion-para-vender/` en vez de anclar `#vender` en la misma página (no hay contenido propio
  de esa finalidad en este hub) — mejor UX, mismo destino funcional.
- Gate `verify.mjs`: corregido el check de credenciales canónicas (aceptaba solo "matrícula N.º"
  contiguo; `CRED_ARQ` tiene "matrícula profesional N.º" en medio) y el de "BCP" (agregado
  "la firma que exige el" e "inscripto en el" como frases seguras, porque `FINALIDADES.credito.corto`
  y la nota del panel de hipotecaria — ambas copy verbatim del spec §3/§5.4 — no calzan con el
  regex original "inscripto en el registro del").
- `tests/wa-menu.mjs`: el check de stacking título/subtítulo se ajustó (compara `y`/`x` de inicio,
  no el borde inferior) porque con 5 opciones el título más largo envuelve a 2 líneas.

**Known issues**
- JSON-LD `Service.offers` con oferta judicial en informes-periciales: sigue sin implementar (ver
  PR-1 known issues).
- `otrasTasaciones` de corporativa y terrenos todavía no linkean a franja de dominio (llega con
  PR-3, cuando se reescriben esas páginas).

**Verification:** `node verify.mjs` PASS, `node tests/wa-menu.mjs` PASS, `grep -c "peritos matriculados" content.mjs` = 0.

## PR-3 — copy verticales (`v3/3-copy-verticales`)

**Built**
- Copy v3 completa en las 5 verticales restantes (casas, departamentos, terrenos,
  locales-comerciales, campos) y en corporativa: `priceBlock` → tabla por finalidad
  (`priceBlockFinalidades`, reemplaza `priceBlockVertical`: cifra grande compraventa + IVA, tres
  filas compraventa/judicial/crédito, `pie: FACTURA_TXT`); `valueBlockShort()` después del precio;
  FAQ con `verticalFaqCore()` (costo por las tres finalidades, plazo con `PLAZO_TXT`, "¿Sirve para
  mi banco o cooperativa?").
- Casas: FAQ "¿La visita tiene costo?" reescrita ("incluida en el precio del informe, para
  cualquier finalidad").
- Corporativa (§5.5): primario → `consulta` con `page.waConsultaText` propio; ítem "Franja de
  Dominio" agregado a "Valuación estratégica de activos"; `otrasTasaciones` gana Franja de Dominio.
- Terrenos: `otrasTasaciones` gana Franja de Dominio (pendiente de PR-2, ver Known issues ahí).
- `docs/routes.json`: `"copyDone": true` — las 15 páginas ya cumplen el contrato v3 completo.
- `PLACEHOLDERS.md` §1/§3/§4 y `plan.md` §1.2/§1.3 actualizados con los hechos confirmados y una
  nota "Superseded 2026-09-10 por prompts/v3-…§1".
- QA visual: capturas de casas, terrenos y corporativa a 1280px (no commiteadas, `docs/screenshots/`
  en `.gitignore`); CI corre su propio job `screenshots` como artifact.

**Decisions**
- `docs/decisions-needed.md` no se crea: D1 (IVA) ya estaba decidido por Anton, no queda ninguna
  pregunta de base pendiente al cierre de v3.

**Known issues**
- JSON-LD `Service.offers` con oferta judicial en informes-periciales: sigue sin implementar
  (cosmético, no afecta ningún hard rule).
- Sin fotografía propia para franja de dominio; reusa `tasacion-terrenos-paraguay` (anotado desde
  PR-1, sigue así).

**Verification:** `node verify.mjs` PASS con `copyDone: true` (gate completo), `node tests/wa-menu.mjs` PASS.

## Zip para Hostinger

Generado en Windows con el método .NET `ZipArchive` (sin `zip` binario disponible), rutas con
`/` verificadas (0 entradas con `\`), 97 archivos, `index.html`/`.htaccess` en la raíz del
archivo (extracción flat sobre `public_html/`). Anton sube el zip a Hostinger; esta sesión nunca
deploya.

dist/tasacion-2026-09-10.zip
