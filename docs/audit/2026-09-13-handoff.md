# Handoff: tasacion.com.py audit fixes (2026-09-13)

Read this first in a new session. Status page (Sonnet): https://claude.ai/code/artifact/b88e9c30-1129-4f69-a201-4a144bb87b59

## Where things stand

- Repo: `C:\Claude 1\tasacion-com-py` (the only current checkout; `C:\Claude 1\tasacion` is a stale copy of master plus the audit docs).
- Branch: `audit-fixes-2026-09-13`, pushed to origin. Based on `v4/1-cache-jsonld-wa` (unreleased) with `origin/master` merged in for the audit docs.
- Live site = master (older). Deploying this branch ships v4 (cache-busting, JSON-LD Offers per purpose, WhatsApp menu target=_blank) plus the audit fixes together.
- Deploy zip built and NOT uploaded: `dist/tasacion-2026-09-13.zip` (98 files, flat, extract inside `public_html/`). `dist/` is gitignored; rebuild with the steps below if missing.
- Nothing is deployed. No coding is left for the fixes already done; the zip is Hostinger-ready as is.

## What was fixed, by whom

All source fixes: Codex CLI `gpt-6-astra`, effort `low`, session `01a098a4-69e9-7f80-a723-6ee9dcf1c921` (verified from the session jsonl). Audited by Claude Fable 5.1. Commit `583c8de`.

| # | Audit item | Fix | Verified by |
|---|---|---|---|
| 2 | Form false success | `lead-forward.php` checks log write and CRM forward; thank-you only if one accepted, else `/contacto/?error=envio` | code review; PHP not runnable locally |
| 3 | Phone error dead anchor | short phone -> `/contacto/?error=telefono`; `site.js` shows role=alert message; `minlength=6` on input | browser: both messages render on localhost |
| 4 | No privacy page | new `/privacidad/` in `content.mjs`, linked in footer and next to form, in sitemap and `docs/routes.json` | browser + verify.mjs |
| 5 | Seller "Mismo informe" contradiction | seller card rewritten: not an official report, cost covered only with exclusivity | grep of built HTML |
| 7 | `#incluye` hero link | already resolves on v4 (section has `id="incluye"`); no change needed | grep |
| 9 | Seller cost claim under-qualified | "con exclusividad" added to seller CTAs | grep |
| QW | Footer year empty until JS | build-time year in `renderFooter` | grep |
| QW | "desde Gs." lowercase paragraph, FAQ article | capitalized; "el informe lo firma el perito tasador" | grep |
| 6 | Mortgage JSON-LD price | already fixed in v4, not touched | verify.mjs |
| 8 | ANALYTICS_ID | left empty on purpose, needs a value from Anton | n/a |

Fable inline (trivial edits): reverted an unrequested honeypot-redirect change by Codex; added `privacidad` to the ship list in `deploy/make-zip.sh` (commit `0d74bf6`). Fable also built the zip with .NET because `zip` is not installed.

## Round 2 (same day, later): done and pushed, commit `eadfe21`

Codex `gpt-6-astra` low, session `01a098d1-7aaa-79a0-867a-bde656cbd551`. Audited by Fable: rebuild, verify.mjs PASS, browser check (contact error with deferred script, local fonts load, no console errors).

- Meta descriptions shortened (hipotecaria 142 chars, valuacion-para-vender 147).
- JSON-LD Service areaServed: Asunción, Gran Asunción, Paraguay (Country).
- `site.js` loaded with `defer`.
- `sitemap.xml` generated from PAGES by `build-site.mjs` (16 URLs unchanged; per-page `lastmod` supported).
- Six unreferenced `tasador-midiendo-*` images deleted.
- Dead code removed: `PRECIO`, `CRED_SELLO_CORTA`, `renderPage` opts, orphan CSS, legacy zonas wiring.
- `/contacto/` copy expanded to 387 words (service area incl. all Paraguay, response time, what to prepare).
- Fonts self-hosted: `assets/fonts/inter-variable.woff2`, `libre-baskerville-700.woff2`, `fonts.css`. Fable downloaded the woff2 files (Codex has no network); Codex wired them. No Google Fonts requests remain in shipped files.
- Anton's decisions: tasacion.com not available, dropped (no mentions in the site). GA4 skipped, stays in plan. Pricing copy: not guessed; review doc for Fernando at `docs/audit/2026-09-13-revision-precios-fernando.md` (artifact https://claude.ai/code/artifact/5dd41b44-ae8c-457f-9cc2-cc690434899a). Privacy notice kept as is (Fernando Capurro, no RUC).
- Zip rebuilt: `dist/tasacion-2026-09-13.zip` (95 files). Not uploaded.

## Still open after round 2 (letters match the status page)

- A. Pricing text edits once Fernando answers the review document.
- B. GA4 or GTM id (Anton provides; trivial).
- C. 1920 webp over 300 KB. Needs sharp, needs npm. Blocked until Node/npm installed (H).
- D. Playwright tests. Blocked until npm installed (H).
- E. One real test lead after deploy: confirm CRM delivery and the error path.
- F. VenderCRM integration: `lead-forward.php` already posts to `VENDERCRM_URL/api/v1/leads` with `X-Api-Key`. Needs env vars `VENDERCRM_URL` and `VENDERCRM_API_KEY` on Hostinger and the site registered in VenderCRM. Use the `vendercrm-lead-capture` skill.
- G. Lead and click visibility: no dashboard today. Leads land in `leads.log` (file in public_html) and in VenderCRM once F is done. WhatsApp clicks only push to a JS dataLayer that nothing reads until B is set. Option if wanted: a small password-protected PHP page listing leads.log.
- H. Install Node LTS on the laptop from https://nodejs.org (Windows installer, tick "add to PATH"), reopen the terminal, then `npm install` in the repo. Unblocks C and D and lets `npm run verify` work without the PATH trick.

## Original open list from round 1 (superseded by the letters above; kept for history)

1. Domain `tasacion.com` shows a parking page. Buy and 301 to `.com.py`, or stop using the name. Not code.
2. `ANALYTICS_ID` empty. Needs a GA4 or GTM id from Anton. Trivial once given.
3. Pricing copy contradiction on `/informes-periciales/` (size does or does not affect price). Small copy edit in `content.mjs`.
4. Meta descriptions too long on `/tasaciones/hipotecaria/` and `/valuacion-para-vender/`. Small.
5. JSON-LD `areaServed` hardcoded to Asunción on `/tasaciones/campos/`. Small, `build-site.mjs`.
6. `tasacion-de-inmuebles-asuncion-1920.webp` is 360 KB. Re-encode under 300 KB. Small, needs sharp (no node_modules here).
7. `site.js` without `defer`. Trivial.
8. Sitemap hand-maintained; generate from PAGES. Medium.
9. Six unreferenced `tasador-midiendo-*` images (310 KB) shipped. Trivial to drop.
10. Dead code: `PRECIO`, `CRED_SELLO_CORTA`, orphan CSS, legacy zonas wiring in `build-images.mjs`. Small.
11. `/contacto/` is thin (222 words). Small copy addition.
12. Self-host Google Fonts. Medium.
13. Run the Playwright tests (needs `npm install`, needs Node on PATH). Medium.
14. Send one real test lead after deploy to confirm CRM delivery and the error path. Manual, after deploy.
15. Privacy notice: confirm responsible-party name and whether to add RUC in the footer. Copy.

## Deploy routes (pick one)

- Zip: upload `dist/tasacion-2026-09-13.zip` in hPanel File Manager, extract inside `public_html/`.
- Git: merge `audit-fixes-2026-09-13` into `master`; Hostinger git integration pulls it. The repo commits built HTML, so this should work, but confirm how the integration is configured for this site before merging.

## Environment facts (this laptop)

- Node, npm, PHP, Python, zip are NOT on PATH. Only node: `C:\Users\anton\AppData\Local\OpenAI\Codex\runtimes\cua_node\a708e72b10c27b59\bin\node.exe` (prepend that folder to PATH before `node build-site.mjs` or `node verify.mjs`).
- Local preview: `C:\Claude 1\.claude\launch.json` has a `tasacion` entry pointing at that node and `serve.mjs`, port 4322. In Claude Code: preview_start name `tasacion`, then open http://localhost:4322/.
- verify.mjs diffs generated HTML against the committed tree, so it only passes after a commit.
- Rebuild zip: run `node build-site.mjs`, stage the SHIP list from `deploy/make-zip.sh` into `dist/tasacion-<date>/`, zip flat with .NET ZipArchive (forward-slash entries).

## Process

Skill `manager-worker-codex` at `~/.claude/skills/manager-worker-codex/`. Fable plans and audits; Codex implements via `scripts/codex-run.ps1 -Repo "C:\Claude 1\tasacion-com-py" -Tier normal -PromptFile <prompt.txt>`. Cheap tier `gpt-5.6-luna` low for mechanical edits, `gpt-6-astra` low for normal work. Never dispatch `gpt-6-astra` high without Anton saying so in the conversation. Resume with `-Resume <session id> -Tier <same tier>`.

## Round 3 (same day): VenderCRM wiring, commit `bfebefd`

Codex `gpt-6-astra` low, session `01a09b0f-5251-7ee0-855f-eea7c5f58132` (2 turns, both verified low from the jsonl). Audited by Fable: build, commit, verify.mjs PASS, `tests/wa-menu.mjs` PASS (Playwright now installed), browser check (one deferred `vc-attribution.js` tag, no console errors).

- `lead-forward.php` loads `../vendercrm-config.php` (one level above `public_html`) when the env vars are empty; env vars still win. Template: `deploy/vendercrm-config.example.php` (not shipped in the zip). Documented in `docs/DEPLOY.md` §3.
- Attribution snippet `https://crm.clientes.com.py/vc-attribution.js` (defer) in every page head.
- `tools/img-size.mjs` added. Item C dropped: the 1920 webp cannot get under 300 KB above webp quality 45 (probe: q70 353 KB, q50 311 KB, q40 273 KB); the 187 KB AVIF is what modern browsers load.
- Node 24.19 / npm 11.17 installed on the laptop via winget (H done). `npm install` and `npx playwright install chromium` done.
- Zip rebuilt: `dist/tasacion-2026-09-13.zip` (95 files). Not uploaded.

Remaining: A (Fernando prices), B (analytics id, optional), E (test lead after deploy), F on the server side (create `vendercrm-config.php` above `public_html` with url `https://crm.clientes.com.py` and the site key; confirm site active and default stage in VenderCRM → Sitios), G optional.
